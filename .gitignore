# Dependencies & build output
# (no trailing slash: also matches a node_modules symlink, which the
# directory-only pattern let slip into the index once)
node_modules
out/
dist/
build/

# Private project instructions — never publish
CLAUDE.md

# Brainstorming / analysis HTML (kept local, not shipped)
brainstorm/
docs/

# Large media (host on the landing page / a GitHub release instead)
assets/*.mp4

# Local caches / exports / scratch
*.log
.DS_Store
Hindcast Exports/
scratchpad/

# Editor
.vscode/
.idea/
